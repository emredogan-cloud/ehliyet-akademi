<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local notifications (flutter_local_notifications) — Phase 5. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Ehliyet Akademi"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Beta Faz 1 — derin bağlantı desteği.
                 Flutter'ın YERLEŞİK derin bağlantı yolu açılır: gelen URI doğrudan go_router'a
                 verilir ve `/davet/<KOD>` rotası eşleşir. Ek bir eklenti (app_links vb.) GEREKMEZ.
                 Bu bayrak olmadan intent-filter'lar uygulamayı açar ama URI yönlendiriciye
                 ULAŞMAZ — uygulama ana sayfada açılır ve kod sessizce kaybolur. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Android App Link (doğrulanmış https).
                 `autoVerify` → Android, kurulumda `https://<alan>/.well-known/assetlinks.json`
                 dosyasını okur ve alan adının bu uygulamaya izin verdiğini doğrular. Doğrulanırsa
                 bağlantı SEÇİM PENCERESİ OLMADAN uygulamada açılır.
                 Doğrulama, imzalama parmak izinin sunucudaki dosyada bulunmasına bağlıdır
                 (bkz. `apps/web/app/.well-known/assetlinks.json/route.ts`). Doğrulanmazsa bağlantı
                 tarayıcıda açılır ve web sayfası kullanıcıyı yine uygulamaya yönlendirir —
                 akış kırılmaz, yalnız bir dokunuş uzar. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="www.ehliyetegitim.com"
                      android:pathPrefix="/davet"/>
                <!-- www'suz alan adı da aynı sayfaya çıkar; ikisi ayrı "host" sayılır. -->
                <data android:scheme="https"
                      android:host="ehliyetegitim.com"
                      android:pathPrefix="/davet"/>
            </intent-filter>
            <!-- Özel şema — doğrulama GEREKTİRMEZ, kurulu uygulamayı her koşulda açar.
                 App Links doğrulaması (sunucu yapılandırması, DNS, önbellek) bizim elimizde
                 olmayan sebeplerle bozulabilir; web sayfasındaki "uygulamada aç" düğmesi bu şemayı
                 kullanabilir. Tek başına yeterli DEĞİLDİR: uygulama kurulu değilse hiçbir şey
                 olmaz, bu yüzden yedek yol olarak durur.

                 `host="app"` ZORUNLU — süs değil. Flutter gelen URI'yi OLDUĞU GİBİ yönlendiriciye
                 verir (`data.toString()`), go_router ise yalnız `uri.path` ile eşleştirir.
                 `ehliyetakademi://davet/<KOD>` yazılsaydı `Uri.parse` "davet"i HOST sayar ve path
                 BOŞ kalırdı → hiçbir rota eşleşmez, uygulama hata rotasında açılırdı. Araya bir
                 host konunca path `/davet/<KOD>` olur ve https biçimiyle AYNI yola düşer.
                 Doğru biçim: `ehliyetakademi://app/davet/<KOD>` (test: `deep_link_test.dart`). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="ehliyetakademi" android:host="app" android:pathPrefix="/davet"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications scheduled-notification receivers (Phase 5). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
