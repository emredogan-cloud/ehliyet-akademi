node_modules/
.next/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/
.env
.env.*
!.env.example
.vercel/
playwright-report/
test-results/
.pglite/
*.log
.DS_Store
.vercel
.env*
# DİKKAT: yukarıdaki `.env*`, satır 10'daki `!.env.example` istisnasını geçersiz kılıyordu (son
# eşleşen kalıp kazanır) — bu yüzden `.env.example` ŞABLONLARI depoya hiç girmemişti. Şablonlarda
# gizli değer yoktur ve depoda bulunmaları GEREKİR (ENV_TEMPLATE.md §3).
!.env.example

# Yerel ajan/araç yapılandırması (repoya girmez)
.claude/
.agents/
skills-lock.json

# Kaynak görsel çalışma dizinleri — uygulamada servis EDİLMEYEN ham girdiler.
# Servis edilen optimize sürümler apps/web/public/assets/ altında (izlenir).
/assets/
/new-image/
/apps/web/public/ui/

# Copyrighted MEB exam-paper archive — reference only, NEVER commit/republish (QIP 2.0 content-expansion)
sınav-soruları-pdf/

# Design references for the UI redesign sprint (mockups + source art) — not shipped, large.
/apps/assets/
# Play Store mağaza varlıkları (simge 512², öne çıkan grafik 1024×500, ekran görüntüleri).
# Play Console'a ELLE yüklenir; ~12 MB ham çıktı, uygulamada servis edilmez. Bkz. PLAY_CONSOLE_SETUP.md §6.
/apps/ASO_IMAGE/
# Loose reference PDFs
/*.pdf
google-services.json
